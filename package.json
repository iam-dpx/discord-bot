{
  "name": "dead-pixel-discord-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Dead Pixel Discord customizer bot (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node register.mjs"
  },
  "dependencies": {
    "discord-interactions": "^3.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "typescript": "^5.4.0"
  }
}
